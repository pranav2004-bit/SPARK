# ═══════════════════════════════════════════════════════════════════════════════
# QUARANTINED — LEGACY MONOLITH
# ═══════════════════════════════════════════════════════════════════════════════
#
# The legacy monolith is quarantined for the testing phase.
# DO NOT start this file. DO NOT route any requests to it.
#
# Code is preserved in:  services/legacy-monolith/
# Compose is archived in: docker-compose.QUARANTINED-legacy-monolith.yml
#
# ── Active microservices stack ───────────────────────────────────────────────
# Start dev:   docker-compose -f infra/docker-compose.dev.yml up --build
# Start prod:  infra/scripts/start-prod.sh
# ═══════════════════════════════════════════════════════════════════════════════

# This file defines NO services intentionally.
# Running `docker-compose up` here starts nothing.
services: {}
