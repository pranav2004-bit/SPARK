# Auto-detect text files and normalize line endings to LF in the repository
* text=auto eol=lf

# Explicitly declare shell scripts as using LF
*.sh text eol=lf

# Batch scripts use CRLF on Windows
*.bat text eol=crlf
*.cmd text eol=crlf
